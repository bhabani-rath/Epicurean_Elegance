<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.FoodOrderApp" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>
    <style name="AppTheme" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="android:windowDrawsSystemBarBackgrounds">true</item>
        <!-- Set the status bar color -->
        <item name="android:statusBarColor">@color/status_bar_color</item>
    </style>
    <style name="AlertDialogStyle" parent="@android:style/Theme.Dialog">
        <item name="android:fontFamily">@font/yeon_sung</item>
        <item name="android:textColor">@color/Base_Theme</item>
        <item name="android:textAppearance">@font/yeon_sung</item>
        <item name="android:textSize">10sp</item>
    </style>
    <style name="LogIn_Back" parent="@android:style/Theme.Dialog">
        <item name="android:fontFamily">@font/yeon_sung</item>
        <item name="android:textColor">@color/Base_Theme</item>
        <item name="android:textAppearance">@font/yeon_sung</item>
        <item name="android:textSize">10sp</item>
    </style>

    <style name="Theme.FoodOrderApp" parent="Base.Theme.FoodOrderApp" />
</resources>